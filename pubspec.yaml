name: bilge_ai
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.4.1 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # iOS stili ikonlar için
  cupertino_icons: ^1.0.8

  # State Management
  flutter_riverpod: ^2.5.1

  # Navigation
  go_router: ^14.1.0

  # Firebase
  firebase_core: ^3.1.1
  firebase_auth: ^5.1.1
  cloud_firestore: ^5.0.2
  firebase_storage: ^12.1.0

  # Grafikler
  fl_chart: ^0.68.0

  # Font
  google_fonts: ^6.2.1
  intl: ^0.19.0
  flutter_animate: ^4.5.0

  http: ^1.2.1
  flutter_markdown: ^0.7.1

  # BENZERSİZ ID İÇİN EKLENDİ
  uuid: ^4.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^4.0.0

# Ana Flutter yapılandırması
flutter:
  uses-material-design: true

  # Şimdilik hiçbir asset kullanmıyoruz, bu yüzden burası temiz kalabilir.
  # assets:
  #   - assets/